import { StoryObj, Meta } from '@storybook/preact'
import { Button } from '@raio-laser/react'

export default {
  title: 'Button',
  component: Button
} as Meta

export const Primary: StoryObj = {
  
}